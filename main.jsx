import React from "react";
import ReactDOM from "react-dom/client";
import AppTreino from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AppTreino />
  </React.StrictMode>
);
